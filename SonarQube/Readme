System requirements 
  Memory: 16GB 
  VCPU: 4 (Recomended 8)
  Volume: 128GB 
  
Run Sonarqube in Docker container
  Pre-requisites: Docker & Docker-compose
  Install Docker & Docker-compose using the below link in Centos/RHEL/Debain/Ubuntu
  https://github.com/devopslife999/Setup_Quick_Access/tree/main/Docker
  
  For Centos/RHEL linux execute "sudo docker compose.yml up -d" to run sonarqube in docker container
  For Debain/Ubuntu linux execute "sudo docker-compose.yml up -d" to run sonarqube in docker container
  Access sonarqube at 8123 port "localhost:8123 or <publicip>:8123"
